import React from 'react';
import ReactDOM from 'react-dom/client';
import MultiPassPage from './pages/MultiPassPage.jsx';
import './App.css';

console.log('🚀 MAIN.JSX LOADED - NEW BUILD VERSION 2024-08-05');

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <MultiPassPage />
    </React.StrictMode>
);